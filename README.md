# production-schedule-optimiser
A prototype to demonstrate how Python and Pandas can automate production scheduling. It reads order data, applies business rules, and pushes the result to a shared Google Sheet.
